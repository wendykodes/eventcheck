// Venue-QR entry renders the shared Guest Journey home (same component as
// the invitation-link entry — one experience, two doors).
import JourneyHome from './JourneyHome';

export default function SelfCheckinPage() {
  return <JourneyHome />;
}
